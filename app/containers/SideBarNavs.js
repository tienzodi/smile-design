// @flow
import { connect } from 'react-redux';
import SideBarNavs from '../components/SideBarNavs/index';

// Redux connection
const mapStateToProps = (state) => {
  return {};
};

const mapDispatchToProps = (dispatch) => {
  return {};
};

export default connect(mapStateToProps, mapDispatchToProps)(SideBarNavs);
